credentials:
  type: csv
  reload: 60
  path: fingerprints.csv
jwt:
  rsaPrivateFile: extauth.rsa
  localIssuer: tlsgw.local
  issuers:
    tlsgw:
      iss: tlsgw.local
      local:
        path: jwks.txt
